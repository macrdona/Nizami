/*Use database*/
use Nizami;

/*Creating table for users*/

create table Users(
	UserID int,
	FirstName varchar(255),
	LastName varchar(255),
	PhoneNumber varchar(255),
	Email varchar(255),
	Salt varchar(255),
	Username varchar(255),
	Password varchar(255),
	PRIMARY KEY (UserID)
);

/*Create table for products*/

create table Products(
	ProductID int,
	Name varchar(255),
	Description varchar(1000),
	Price decimal,
	Category varchar(255),
	Image varchar(1000),
	PRIMARY KEY (ProductID)
);


/*Adding Tables Orders and Cartline*/

EntityFrameworkCore\Add-Migration Orders
and you do EntityFrameworkCore\Update-Database afterwards


/*Update Table Orders to add Shipping*/

Alter table Orders
add Shipping bit;
