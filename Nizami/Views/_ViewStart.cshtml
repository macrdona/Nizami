@{

    var routeUrl = ViewContext.RouteData.Values["action"].ToString();

    if (routeUrl.Contains("Index") || routeUrl.Contains("Privacy"))
    {
        Layout = "_IndexLayout";
    }
    else{
        Layout = "_CartLayout";
    }
}
